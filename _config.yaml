url: https://aiceblock.eu
baseurl: ""
description: Make your AI systems audit-ready — from day one. aiceblock structures your AI lifecycle around the AI Act — from risk classification to documented evidence — so you can build, deploy, and prove compliance with confidence.

title: "aiceblock"
favicon:
  src: "/assets/images/favicon.ico"
  type: "image/x-icon"

plugins:
  - jekyll-dotenv
  - jekyll-polyglot

exclude: ["README.md"]

# I18n
languages: ["en", "pt-PT"]
default_lang: "en"
exclude_from_localization: ["javascript", "images", "css", "public"]
parallel_localization: true

# Default WEB3FORM key associated with info@aiceblock.com
# If .env isn't set it defaults to this key
web3forms_access_key: "f3c588cf-4beb-40a2-b94f-54a4f13936d1"
